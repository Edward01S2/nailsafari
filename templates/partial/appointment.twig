<section id="appointment" class="">
    <div class="bg-bottom bg-fixed bg-cover" style="background-image: url({{post.get_field('appt_bg')}});">
        <div class="container mx-auto flex justify-center md:justify-end">
            <div class="text-center py-12 flex-col md:text-left md:pr-16 md:py-16 lg:py-32">
                <p class="pb-5 text-white font-bold text-2xl text-shadow md:text-5xl">{{post.get_field('appt_text')}}</p>
                <a class="text-2xl align-middle bg-primary hover:bg-secondary text-white font-bold py-2 px-4 rounded-full no-underline ml-1 md:text-3xl" href="tel:{{options.phone}}">{{post.get_field('appt_btn')}}</a>
            </div>
        </div>
    </div>
</section>