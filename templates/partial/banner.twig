<section id="banner" class="text-center text-white bg-primary p-4">
    <div class="mx-auto py-2">
        <p class="text-lg lg:text-xl">{{ post.get_field('banner_text')}} <a class="text-2xl align-middle bg-secondary hover:bg-white hover:text-primary hover:border-primary text-white font-bold py-2 px-4 rounded-full no-underline ml-1" href="tel:{{options.phone}}<">{{options.phone}}</a>
            <span class="hidden md:inline-block lg:ml-2"> to book an appointment!</span>
        </p>
    </div>
</section>